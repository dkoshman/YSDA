[tool.poetry]
name = "my_tools"
description = "My package for machine learning tasks."
version = "0.1.0"
authors = ["Dima Koshman <koshmandk@yandex.ru>"]

[tool.poetry.dependencies]
python = ">=3.7"

[build-system]
requires = ["poetry_core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
